version: "3.9"

services:

  backend:
    build: ./Backen/flaskp
    container_name: flask_backend

    ports:
      - "5000:5000"

  frontend:
    build: ./FrontEnd
    container_name: express_frontend

    ports:
      - "3000:3000"

    depends_on:
      - backend